# Brew
alias bubo="brew update; brew outdated"
alias bubc="brew upgrade; brew cleanup -s --force"
alias brfull="bubo; bubc; brew doctor"

# General
alias rm="rm -i"
alias clear=" clear"
alias pwd=" pwd"
alias exit=" exit"

# Git
alias ggpp="git push origin +HEAD"

# Shopify
alias glup='git pull && dev up'
